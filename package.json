{
  "name": "sign-aura",
  "version": "1.0.0",
  "description": "A real-time sign language recognition app with support for multiple languages",
  "main": "app.html",
  "scripts": {
    "start": "serve -s . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
